## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | Icons for trust badges and UI elements
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The application uses standard Shadcn UI components which should already be installed.
All images are sourced dynamically from Unsplash via URLs.
